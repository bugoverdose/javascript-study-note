[index.js]
import { GraphQLServer } from "graphql-yoga";
import resolvers from "./graphql/resolvers";

const server = new GraphQLServer({
  // 스키마 지정. typeDefs, resolvers 등
});

server.start(() =>
  console.log("GraphQL Server Running on http://localhost:4000/")
);

====================================================================