const HOME = "/";
const LOGIN = "/login";
const PHOTOS = "/photos";
const PROFILE = "/profile";

const routes = {
  home: HOME,
  login: LOGIN,
  photos: PHOTOS,
  profile: PROFILE,
};

export default routes;
